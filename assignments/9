#include <stdio.h>
#include <conio.h>

int main(int argc, char const *argv[])
{
    int number, ognumber, remainder, result = 0;
    printf("Enter a three digit number:");
    scanf("%d", &number);
    ognumber = number;

    while(ognumber != 0)
    {
        remainder = ognumber%10;
        result = remainder*remainder*remainder;
        ognumber /= 10;
    }
    if(result == number)
    {
        printf("The number is an armstrong number");
    }
    else
    {
        printf("The number is not an armstrong number");
    }
    return 0;
}
#include <stdio.h>
#include <conio.h>

int main(int argc, char const *argv[])
{
    int marks, Distinction, FirstDivision, SecondDivision, ThirdDivision;
    printf("Enter the marks:");
    scanf("%d", &marks);
    
    while(marks > 0 )
    {
        
        if(marks >= 75 && marks >= 40)
        {
            printf("Congrats, you got distinction");
            break;
        }
        else if(marks >= 60 && marks<75  && marks >= 40 )
        {
            printf("You got first division");
            break;
        }
        else if(marks < 60 && marks >= 50 && marks >= 40 )
        {
            printf("you got second division");
            break;
        }
        else if (marks < 50 && marks >= 40 )
        {
            printf("you got third division");
            break;
        }
        else
        {
            printf("You are failed!");
            break;
        }
    }
    return 0;
}
